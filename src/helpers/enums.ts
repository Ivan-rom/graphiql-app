export enum Routes {
  signIn = 'sign-in',
  signUp = 'sign-up',
}

export enum SignUpInputsNames {
  name = 'name',
  email = 'email',
  password = 'password',
  confirmPassword = 'confirmPassword',
}

export enum SignInInputsNames {
  email = 'email',
  password = 'password',
}
